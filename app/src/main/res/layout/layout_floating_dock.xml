<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/dock_container"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    app:cardCornerRadius="24dp"
    app:cardElevation="2dp"
    app:strokeWidth="0.5dp"
    app:strokeColor="#1A000000"
    app:cardBackgroundColor="?attr/colorSurface"
    app:useCompatPadding="false">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:orientation="horizontal"
        android:paddingHorizontal="8dp"
        android:gravity="center_vertical">

        <!-- Home Pivot -->
        <FrameLayout
            android:id="@+id/dock_home"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless">
            <ImageView
                android:id="@+id/dock_home_icon"
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_nav_home_outline"
                app:tint="?attr/colorPrimary" />
            <View
                android:id="@+id/dock_home_indicator"
                android:layout_width="12dp"
                android:layout_height="2dp"
                android:layout_gravity="bottom|center_horizontal"
                android:layout_marginBottom="10dp"
                android:background="@drawable/bg_vertical_bar_primary" />
        </FrameLayout>

        <!-- Studio Create (Center Action - Soft Pill Style) -->
        <FrameLayout
            android:id="@+id/dock_create"
            android:layout_width="64dp"
            android:layout_height="44dp"
            android:layout_marginHorizontal="6dp"
            android:clickable="true"
            android:focusable="true">
            <View
                android:id="@+id/dock_create_bg"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/bg_pill_soft"
                android:backgroundTint="?attr/colorPrimary" />
            <ImageView
                android:id="@+id/dock_create_icon"
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:layout_gravity="center"
                android:src="@drawable/rounded_add_24"
                app:tint="@color/white" />
        </FrameLayout>

        <!-- Documents Hub -->
        <FrameLayout
            android:id="@+id/dock_documents"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless">
            <ImageView
                android:id="@+id/dock_documents_icon"
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_typcn_document"
                app:tint="?attr/colorPrimary"
                android:alpha="0.3" />
            <View
                android:id="@+id/dock_documents_indicator"
                android:layout_width="12dp"
                android:layout_height="2dp"
                android:layout_gravity="bottom|center_horizontal"
                android:layout_marginBottom="10dp"
                android:background="@drawable/bg_vertical_bar_primary"
                android:visibility="gone" />
        </FrameLayout>

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
